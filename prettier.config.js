module.exports = {
  semi: true,             // Add semicolons at the end of statements (default: true)
  singleQuote: true,       // Use single quotes instead of double quotes
  trailingComma: 'es5',    // Add trailing commas where valid in ES5 (objects, arrays, etc.)
  tabWidth: 2,             // Set the number of spaces per indentation level
  useTabs: false,          // Indent with spaces instead of tabs
  bracketSpacing: true,    // Add spaces between brackets in object literals (default: true)
  printWidth: 80,          // Wrap lines that exceed 80 characters
  jsxSingleQuote: false,   // Use double quotes in JSX
  arrowParens: 'always',   // Include parentheses around a sole arrow function parameter (default: "always")
};
